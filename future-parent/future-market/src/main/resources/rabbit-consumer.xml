<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/rabbit
           http://www.springframework.org/schema/rabbit/spring-rabbit.xsd
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd">

    <rabbit:connection-factory id="connectionFactory" host="localhost"/>

    <rabbit:template id="amqpTemplate" connection-factory="connectionFactory"
        exchange="myExchange" />
    
    <rabbit:admin connection-factory="connectionFactory" />
    
    <rabbit:queue id="marketQ" />
    
    <!-- 广播 -->
    <rabbit:fanout-exchange name="myExchange">
        <rabbit:bindings>
            <rabbit:binding queue="marketQ"></rabbit:binding>
        </rabbit:bindings>
    </rabbit:fanout-exchange>
    
    
    <rabbit:listener-container connection-factory="connectionFactory">
        <rabbit:listener ref="messageHandle" method="onMessage" queues="marketQ" />
    </rabbit:listener-container>
    
    <bean id="messageHandle" class="com.future.market.message.MarketHandle" />
    
</beans>