<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/rabbit
       http://www.springframework.org/schema/rabbit/spring-rabbit.xsd">
       
    <rabbit:annotation-driven/>
    
    <rabbit:connection-factory id="connectionFactory" host="localhost"/>
    <rabbit:admin connection-factory="connectionFactory" />
    
    <rabbit:template id="marketTemplate" connection-factory="connectionFactory"
        exchange="com.future.market" message-converter="messageConverter"/>
    
    <rabbit:topic-exchange name="com.future.market">
    </rabbit:topic-exchange>
    
    <rabbit:queue id="marketQ"></rabbit:queue>
    
    <!-- 设置监听 -->
    <rabbit:listener-container connection-factory="connectionFactory" message-converter="messageConverter" 
        <rabbit:listener ref="messageHandle" queues="marketQ"/>
    </rabbit:listener-container>
    
<!--     <bean id="listenerContainer" class="org.springframework.amqp.rabbit.listener.SimpleMessageListenerContainer">
        <property name="queues" ref=""></property>
        <property name="connectionFactory" ref="connectionFactory"></property>
        <property name="messageListener" ref="messageListener"></property>
    </bean>
    
    消息监听
    <bean id="messageListener" class="org.springframework.amqp.rabbit.listener.adapter.MessageListenerAdapter">
        <constructor-arg index="0" ref="messageHandle">
        </constructor-arg>
        <constructor-arg index="1" ref="messageConverter">
        </constructor-arg>
    </bean> -->
    
    <!-- 消息处理 -->
    <bean id="messageHandle" class="com.future.market.service.mq.MessageHandle" />
       
    <!-- json转换器，消息可以自动根据转换器转换格式，不配置时默认为java序列化，可以自行配置 -->
    <bean id="messageConverter" class="org.springframework.amqp.support.converter.Jackson2JsonMessageConverter" />
</beans>       
